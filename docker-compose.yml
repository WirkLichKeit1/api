services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: dev
      SECRET_KEY: ${SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL}
    volumes:
      - .:/app
    restart: unless-stopped